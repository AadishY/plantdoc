
// This file is now empty as the SVG animation is being removed
// Component code is kept for reference but not used in the app anymore
import React from 'react';

const RotatingLeaf = () => {
  return null;
};

export default RotatingLeaf;
